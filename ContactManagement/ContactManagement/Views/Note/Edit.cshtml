
@model ContactManagement.DTOs.AddNoteDTO

@{
    ViewBag.Title = "Edit Note";
}

<h2>Edit Note</h2>

@using (Html.BeginForm("Edit", "Note", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.HiddenFor(model => model.NoteId)
        @Html.HiddenFor(model => model.ContactId)

        <label for="Content">Note Content:</label>
        @Html.TextAreaFor(model => model.Content, new { @class = "form-control", rows = 5 })
        @Html.ValidationMessageFor(model => model.Content, "", new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a href="@Url.Action("Index", "Note", new { id = Model.ContactId })" class="btn btn-secondary">Cancel</a>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
